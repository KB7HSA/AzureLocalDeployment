$password = ConvertTo-SecureString '<password>' -AsPlainText -Force
$user = "CeriumDeploy"
New-Object System.Management.Automation.PSCredential ($user, $password)
New-HciAdObjectsPreCreation -AzureStackLCMUserCredential (Get-Credential) -AsHciOUName "OU=AZLocal,DC=CeriumDemo,DC=local"
